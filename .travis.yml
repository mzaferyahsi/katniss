dist: trusty
language: node_js
node_js:
  - "10"

cache: npm

before_install:
  - "npm i -g cross-env codecov"

install:
  - cd agent && npm install
  - cd ../
  - cd app && npm install
  - cd ../

script:
  - cd agent && npm run travis-test
  - cd ../
  - cd app && npm run travis-test
  - cd ../